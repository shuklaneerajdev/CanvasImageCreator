<p id="notice"><%= notice %></p>

<h1>Imagequotes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @imagequotes.each do |imagequote| %>
      <tr>
        <td><%= imagequote.name %></td>
        <td><%= imagequote.value %></td>
        <td><%= link_to 'Show', imagequote %></td>
        <td><%= link_to 'Edit', edit_imagequote_path(imagequote) %></td>
        <td><%= link_to 'Destroy', imagequote, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Imagequote', new_imagequote_path %>
